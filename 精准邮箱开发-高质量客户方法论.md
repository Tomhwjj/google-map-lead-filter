# 精准邮箱开发方法论 — 高质量客户（skill11 起）

> 2026-09-13 总结。来源：skill1-10 批次复盘（210 家触达仅 4 家转化）+ skill11 组建实践 + 分波次触达策略讨论。

## 一、核心思路

不做广撒网，只打**高质量、可核验、未触达**的经销商：

- **客户质量**：grade=A 分销商，官网/品牌背调确认在光伏行业有真实业务
- **邮箱质量**：不是"官网上有过字符串"，而是从联系页/impressum/kontakt/bok 页实抓、清洗核验过的可送达地址
- **触达质量**：逐封、分波、可归因，而不是一次性群发

skill1-10 的教训：批次质量参差（含非 A 级、非分销商）时，转化率 4/210≈2%；纯 A 级的 skill1 批次贡献了大部分转化。**名单质量 > 发送数量**。

## 二、客户筛选漏斗

1. **数据源**：只认正经获客链路（Maps 抓取 → 全量背调 → leads.db）。测试导入数据（如 50 家德国 LDDE- 公司）一律不运营。
2. **筛选条件**：`grade=A AND customer_type=distributor AND pool=潜在客户(未联系) AND 未出现在任何 gmail_contacts 批次`
3. **邮箱补齐**：缺邮箱的先跑官网全量背调（contact/kontakt/impressum/bok 联系页），清洗后写回 `companies.email` + 重算分数 + diffs 审计轨迹，跑前备份。
4. **过滤核验**：剔除模板域（<name@domain.de> 之类）、修复断头邮箱（'720-50info@'→'info@'）、逐条人工过目后入库。
5. **分组**：核验通过后固定归入 Google 联系人组（skill11），后续波次都从这个组走。

## 三、触达策略：分波次轮打

**前提认知**：

- 一家企业的多个邮箱常是同一收件箱的别名，同一时刻多发被看见增量≈0，还附赠群发标签。
- 一次全发如果整体进垃圾箱，是所有邮箱同时阵亡，没有补救机会。
- **回复=转化**，不做过度归因；未回复分波换箱再试，而不是一次定生死。

**波次设计**：

| 波次    | 时间           | 打谁                                                         | 说明          |
| ----- | ------------ | ---------------------------------------------------------- | ----------- |
| 第 1 波 | Day 0        | 每企业 1 个主邮箱（角色箱 sales/orders/export > 通用箱 info/kontakt/bok） | 正常开发信       |
| 第 2 波 | 第 1 波后 4-5 天 | 未回复未弹回的企业，换备用邮箱（个人名义箱 > info@）                             | 同主题换措辞或换切入点 |
| 第 3 波 | 再过 1-2 周     | 仍无动静的                                                      | 最后一次，之后放弃   |

**每波执行纪律**：

- **逐封单独发，不用密送群发**（十几家的量级完全做得到）：每封改称呼、改首段，降低内容雷同度。
- **错峰发送**：一天 10-20 封，分上午下午，不一小时连发。
- **bounce 即停换箱**：某地址弹回，下一波自动换该企业邮箱队列里的下一个（备用邮箱的价值在换箱，不在同时多发）。
- **回复即出列**：任何一波收到回复，该企业退出后续所有波次。

## 四、反垃圾箱要点

- 同域名多地址不同时出现在同一封/同一批
- 波与波之间间隔 ≥4 天，制造"正常商务跟进"节奏
- plain text 优先，无附件无大图，真实签名，标题避免全大写/感叹号
- 发送账号保持低量、稳定、有来有往（回复正常回），养账号信誉
- 弹回地址立即停用（mark_email_invalid），持续给死地址发信会拉低账号信誉

## 五、话术两分（skill11 实例）

| 类型   | 画像                         | 主打                     |
| ---- | -------------------------- | ---------------------- |
| 补货盘  | 已在卖 Deye                   | 库存深度、供货稳定性、价格优势        |
| 切渠道盘 | 卖竞品（GoodWe/SolarEdge）或品牌未知 | Deye 渠道利润率、独家区域政策、供货周期 |

话术由运营者（用户）自己写，不代拟。

## 六、反馈闭环（已建成，自动化）

- **每天 19：00 定时拉信**（Gmail OAuth readonly + 代理 33210）→ R1-R7 瀑布分类入库 email_review（bounce/auto_reply/spam/closed/normal_reply/consumer/unrelated）
- **bounce**：自动 mark_email_invalid，下一波换箱依据
- **正常回复**：出列，人工跟进，换池 100% 人工（change_pool 只提议不执行）
- **下一波名单**：从 email_review 筛「已发、无回复、无 bounce」自动生成，不用手工对

## 七、配套基建

- `data/leads.db`（11 表，WAL，禁裸 SQL，只走 core.py 函数）
- `scripts/email_pipeline_wb.py`：R1-R7 分类管线（dry-run 支持）
- `scripts/gmail_fetch_wb.py`：拉信（trust_env=False + 显式代理 33210）
- `scripts/rerun_emails.py`：官网邮箱重抓/清洗/回写（--from-json / --apply / --dry-run）
- `scripts/sync_skill11.py`：主邮箱提取 → 联系人组同步（幂等可重跑）
- 通信板 `对接-workbuddy.md`：WorkBuddy ↔ Claude Code 协作留痕

## 八、红线（不变）

- 客户池换池 100% 人工
- 德国测试数据（LDDE-）不进批次、不补邮箱、不背调
- 写边界白名单：email_review 主写 + email_anomalies/gmail_contacts.status 仅 bounce
- confidence<0.7 强制 review
